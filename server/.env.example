# MongoDB connection string (local or Atlas)
MONGODB_URI=mongodb://localhost:27017/artlor

# Server port
PORT=4000

# Frontend URL for CORS
CLIENT_URL=http://localhost:5173

# Matching timeout in seconds (request auto-moves to next artist)
MATCH_TIMEOUT_SECONDS=30
